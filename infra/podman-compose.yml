version: "3"

services:
  db:
    image: docker.io/pgvector/pgvector:pg15
    environment:
      POSTGRES_USER: lyra
      POSTGRES_PASSWORD: lyra
      POSTGRES_DB: lyra
    ports:
      - "5432:5432"
